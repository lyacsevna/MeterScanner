# MeterScanner 📱

MeterScanner — это Android-приложение на Kotlin, написанное для упрощения процесса считывания показаний счетчиков (воды, газа, электроэнергии) с помощью камеры телефона. Проект автоматизирует рутинный сбор данных, преобразуя изображения в цифровые значения и предоставляя инструменты для анализа статистики.

## ✨ Основные возможности

*   📸 **Распознавание показаний**: Использует возможности камеры для захвата и анализа показаний счетчиков.
*   🤖 **Автоматическое определение**: Применяет алгоритмы компьютерного зрения для идентификации цифр на циферблатах.
*   📊 **Анализ и статистика**: Ведет историю показаний, строит графики потребления и предоставляет аналитику.
*   💾 **Локальное хранение**: Данные сохраняются в локальной базе данных на устройстве.
*   🎯 **Простой интерфейс**: Минималистичный и интуитивно понятный UI для быстрой работы.

## 🚀 Начало работы

### Предварительные требования

*   Android Studio (версия Flamingo или выше).
*   Устройство Android с API уровня 26 (Android 8.0) или выше.
*   Устройство с камерой для тестирования функции сканирования.

### Установка

1.  Склонируйте репозиторий:
    ```bash
    git clone https://github.com/lyacsevna/MeterScanner.git
    ```
2.  Откройте проект в Android Studio.
3.  Дождитесь завершения синхронизации проекта и загрузки зависимостей Gradle.
4.  Соберите проект (Build > Make Project) для проверки.
5.  Запустите приложение на эмуляторе или физическом устройстве (Run > Run 'app').

## 📁 Структура проекта

```
MeterScanner/
├── app/                    # Основной модуль приложения
│   ├── src/main/java/     # Исходный код (Kotlin)
│   ├── src/main/res/      # Ресурсы (макеты, строки, изображения)
│   └── build.gradle.kts   # Конфигурация сборки модуля
├── docs                    # Файлы расчетно-появснительной записки
├── gradle/                 # Файлы оболочки Gradle
└── build.gradle.kts       # Конфигурация сборки корневого уровня
```

## 🛠 Технологический стек

*   **Язык:** Kotlin 100%.
*   **Архитектура:** MVVM (Model-View-ViewModel) для разделения ответственности.
*   **Камера:** Использует CameraX API для стабильной и простой работы с камерой.
*   **Машинное обучение:** Для распознавания цифр может использоваться ML Kit или аналогичные библиотеки.
*   **База данных:** Room Persistence Library для надежного локального хранения.
*   **Асинхронность:** Kotlin Coroutines и Flow для фоновых операций.

## 🔧 Сборка и разработка

Проект использует систему сборки Gradle с Kotlin DSL (`*.gradle.kts`). Основные зависимости, вероятно, включают:

*   AndroidX библиотеки (AppCompat, ConstraintLayout, Lifecycle).
*   CameraX (core, preview, lifecycle).
*   Room (runtime, compiler, ktx).
*   ML Kit Vision (текстовое распознавание).

Для сборки и запуска приложения на устройстве используйте стандартные инструменты Android Studio.

## 🤝 Вклад в проект

Вклады приветствуются! Если вы хотите улучшить MeterScanner:

1.  Форкните репозиторий.
2.  Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`).
3.  Внесите изменения и закоммитьте (`git commit -m 'Add some amazing feature'`).
4.  Запушьте ветку (`git push origin feature/amazing-feature`).
5.  Откройте Pull Request.

